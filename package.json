{
  "name": "team-builders",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "server",
    "ml-service"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\" \"npm run dev --workspace ml-service\"",
    "install:all": "npm install --workspaces"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}